<?xml version="1.0" encoding="utf-8" ?>
<ContentPage 
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
    xmlns:vm="clr-namespace:RightToLeftStringFormat"
    x:Class="RightToLeftStringFormat.MainPage"
    xmlns:ios="clr-namespace:Xamarin.Forms.PlatformConfiguration.iOSSpecific;assembly=Xamarin.Forms.Core" 
    ios:Page.UseSafeArea="true"
    FlowDirection="RightToLeft">
    <ContentPage.BindingContext>
        <vm:ViewModel/>
    </ContentPage.BindingContext>
    <Frame BackgroundColor="#2196F3" Margin="20" Padding="24" CornerRadius="10">
        <StackLayout>
            <Label Text="{Binding StringFormat1}" TextColor="White" FontSize="Small" />
            <Label Text="{Binding StringFormat2}" TextColor="White" FontSize="Small" />
            <Label Text="{Binding StringFormat3}" TextColor="White" FontSize="Small" />
        </StackLayout>
    </Frame>
</ContentPage>